// Given 2 or more arrays, return the symmetric difference
// such that [1,2,5], [2,5,6] would return [1,6]

function sym(args) {
    console.log(arguments);

    var len = arguments.length;
    //TODO: use .reduce()

};

sym([1,2,5], [2,5,6])